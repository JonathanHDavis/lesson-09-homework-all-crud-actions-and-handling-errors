<h1>New Article</h1>

<% if @article.errors.any? %>

<div id="error_explanation">

  <p><%= pluralize(@article.errors.count, "error") %> prohibited this course from being saved:</p>

  <p>
  <% @article.errors.full_messages.each do |message| %>
    <%= message %>
  <% end %>  
  </p>

</div>

<% end %>

<%= render 'form' %>

<%= link_to "All Articles", articles_path %>